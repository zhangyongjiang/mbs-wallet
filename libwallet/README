libwallet is a client side library for wallet functionality such as
working with addresses, deterministic wallets and different key formats.
libwallet was originally created after forking the non-critical code
from libbitcoin into a separate library.

